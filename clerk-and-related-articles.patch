diff --git a/astro.config.mjs b/astro.config.mjs
index 2c54ae1..c04c764 100644
--- a/astro.config.mjs
+++ b/astro.config.mjs
@@ -1,6 +1,7 @@
 import { defineConfig } from "astro/config"
 import tailwindcss from "@tailwindcss/vite"
 import cloudflare from '@astrojs/cloudflare';
+import clerk from "@clerk/astro"
 
 // https://astro.build/config
 export default defineConfig({
@@ -12,6 +13,17 @@ export default defineConfig({
     //TODO - Remove this as it's for dev only
     prerenderEnvironment: "node",
   }),
+  integrations: [
+    clerk({
+      // Sends unauthenticated /admin/* visitors to our own branded
+      // sign-in page instead of Clerk's hosted Account Portal.
+      signInUrl: "/admin/login",
+      signInFallbackRedirectUrl: "/admin",
+      // UserButton's sign-out redirect is configured globally, not per-component
+      // (the old per-button afterSignOutUrl prop was removed upstream).
+      afterSignOutUrl: "/admin/login",
+    }),
+  ],
   vite: {
     plugins: [tailwindcss()],
   },
diff --git a/package.json b/package.json
index 7738536..ea2e7ef 100644
--- a/package.json
+++ b/package.json
@@ -14,6 +14,7 @@
   },
   "dependencies": {
     "@astrojs/cloudflare": "^14.1.3",
+    "@clerk/astro": "^4.0.2",
     "@lucide/astro": "^1.18.0",
     "@neondatabase/serverless": "^1.1.0",
     "@tailwindcss/vite": "^4.1.4",
diff --git a/src/env.d.ts b/src/env.d.ts
index 79cade3..c0cdc70 100644
--- a/src/env.d.ts
+++ b/src/env.d.ts
@@ -1,17 +1,7 @@
 /// <reference types="astro/client" />
 
-declare namespace App {
-    interface Locals {
-        user?: {
-            id: number
-            email: string
-        }
-    }
-}
-
 interface ImportMetaEnv {
     readonly DATABASE_URL: string;
-    readonly JWT_SECRET: string;
-    readonly ADMIN_EMAIL: string;
-    readonly ADMIN_PASSWORD: string;
+    readonly PUBLIC_CLERK_PUBLISHABLE_KEY: string;
+    readonly CLERK_SECRET_KEY: string;
 }
\ No newline at end of file
diff --git a/src/layouts/AdminLayout.astro b/src/layouts/AdminLayout.astro
index dc98d0e..be83725 100644
--- a/src/layouts/AdminLayout.astro
+++ b/src/layouts/AdminLayout.astro
@@ -1,6 +1,7 @@
 ---
 import "../styles/global.css"
 import { appConfig } from "../constants"
+import { OrganizationSwitcher, UserButton } from "@clerk/astro/components"
 
 interface Props {
   title?: string
@@ -28,19 +29,24 @@ const { title = "Admin", showChrome = true } = Astro.props
     {showChrome && (
       <header class="border-b border-border bg-background">
         <div class="mx-auto flex max-w-5xl items-center justify-between px-6 py-4">
-          <a href="/admin" class="flex items-center gap-2" aria-label="Admin dashboard">
-            <span class="flex h-8 w-8 items-center justify-center rounded-md bg-accent font-heading text-base font-extrabold text-white">7</span>
-            <span class="font-heading text-base font-800 text-foreground">Admin Dashboard</span>
-          </a>
+          <div class="flex items-center gap-2">
+            <span class="flex h-8 w-8 items-center justify-center rounded-md bg-accent font-heading text-base font-extrabold text-white" aria-hidden="true">7</span>
+            <OrganizationSwitcher
+              hidePersonal={true}
+              afterSelectOrganizationUrl="/admin"
+              appearance={{
+                elements: {
+                  rootBox: "flex items-center",
+                  organizationSwitcherTrigger: "font-heading text-base font-800 text-foreground px-2 py-1 rounded-md hover:bg-muted",
+                },
+              }}
+            />
+          </div>
           <div class="flex items-center gap-4">
             <a href="/blog" target="_blank" class="hidden text-sm font-500 text-muted-foreground transition-colors hover:text-accent sm:inline">
               View blog ↗
             </a>
-            <form method="POST" action="/api/logout">
-              <button type="submit" class="rounded-md border border-border px-3 py-1.5 text-sm font-600 text-foreground transition-colors hover:border-accent hover:text-accent">
-                Sign out
-              </button>
-            </form>
+            <UserButton />
           </div>
         </div>
       </header>
diff --git a/src/lib/auth.ts b/src/lib/auth.ts
deleted file mode 100644
index c756637..0000000
--- a/src/lib/auth.ts
+++ /dev/null
@@ -1,91 +0,0 @@
-import type { AstroCookies } from "astro"
-import { sql } from "./db"
-
-const COOKIE_NAME = "admin_session"
-const SESSION_DAYS = 7
-
-export interface AuthUser {
-  id: number
-  email: string
-}
-
-/** 256 bits of randomness from the Web Crypto API — native on Cloudflare
- *  Workers, no node:crypto / nodejs_compat flag required. */
-function generateToken(): string {
-  const bytes = new Uint8Array(32)
-  crypto.getRandomValues(bytes)
-  return Array.from(bytes, (b) => b.toString(16).padStart(2, "0")).join("")
-}
-
-/** Only a SHA-256 hash of the token is ever stored/compared — a DB leak
- *  alone can't be used to log in. */
-async function hashToken(token: string): Promise<string> {
-  const data = new TextEncoder().encode(token)
-  const digest = await crypto.subtle.digest("SHA-256", data)
-  return Array.from(new Uint8Array(digest), (b) => b.toString(16).padStart(2, "0")).join("")
-}
-
-/** Verifies email + password. Hashing/verification happens inside Postgres
- *  via pgcrypto's crypt(), so no bcrypt library is needed in the Worker. */
-export async function verifyCredentials(email: string, password: string): Promise<AuthUser | null> {
-  if (!email || !password) return null
-  const rows = (await sql`
-    SELECT id, email FROM users
-    WHERE email = ${email.toLowerCase().trim()}
-      AND password_hash = crypt(${password}, password_hash)
-    LIMIT 1
-  `) as AuthUser[]
-  return rows[0] ?? null
-}
-
-/** Creates a server-side session record and sets the session cookie. */
-export async function createSession(cookies: AstroCookies, userId: number): Promise<void> {
-  const token = generateToken()
-  const tokenHash = await hashToken(token)
-  const expiresAt = new Date(Date.now() + SESSION_DAYS * 24 * 60 * 60 * 1000)
-
-  await sql`
-    INSERT INTO sessions (user_id, token_hash, expires_at)
-    VALUES (${userId}, ${tokenHash}, ${expiresAt.toISOString()})
-  `
-
-  cookies.set(COOKIE_NAME, token, {
-    httpOnly: true,
-    secure: true,
-    sameSite: "lax",
-    path: "/",
-    maxAge: SESSION_DAYS * 24 * 60 * 60,
-  })
-}
-
-/** Returns the authenticated user for this request, or null. An expired/
- *  revoked session clears the stale cookie. */
-export async function getSessionUser(cookies: AstroCookies): Promise<AuthUser | null> {
-  const token = cookies.get(COOKIE_NAME)?.value
-  if (!token) return null
-
-  const tokenHash = await hashToken(token)
-  const rows = (await sql`
-    SELECT u.id, u.email FROM sessions s
-    JOIN users u ON u.id = s.user_id
-    WHERE s.token_hash = ${tokenHash} AND s.expires_at > now()
-    LIMIT 1
-  `) as AuthUser[]
-
-  if (!rows[0]) {
-    cookies.delete(COOKIE_NAME, { path: "/" })
-    return null
-  }
-  return rows[0]
-}
-
-/** Revokes the session in the database, not just the cookie — a stolen
- *  cookie can't keep working after logout. */
-export async function destroySession(cookies: AstroCookies): Promise<void> {
-  const token = cookies.get(COOKIE_NAME)?.value
-  if (token) {
-    const tokenHash = await hashToken(token)
-    await sql`DELETE FROM sessions WHERE token_hash = ${tokenHash}`
-  }
-  cookies.delete(COOKIE_NAME, { path: "/" })
-}
\ No newline at end of file
diff --git a/src/middleware.ts b/src/middleware.ts
index 791a9c9..0135a98 100644
--- a/src/middleware.ts
+++ b/src/middleware.ts
@@ -1,44 +1,23 @@
-import { defineMiddleware } from "astro:middleware"
-import { getSessionUser } from "./lib/auth"
-
-export const onRequest = defineMiddleware(async (context, next) => {
-  const { pathname } = context.url
-
-  const isAdminPage =
-      (pathname === "/admin" || pathname.startsWith("/admin/")) &&
-      pathname !== "/admin/login"
-  const isProtectedApi = pathname.startsWith("/api/articles")
-
-  if (isAdminPage || isProtectedApi) {
-    let user = null
-
-    try {
-      user = await getSessionUser(context.cookies)
-    } catch (err) {
-      console.error("[middleware] session check failed:", err)
-    }
-
-    if (!user) {
-      if (isProtectedApi) {
-        return new Response(JSON.stringify({ error: "Unauthorized" }), {
-          status: 401,
-          headers: { "Content-Type": "application/json" },
-        })
-      }
-      return context.redirect("/admin/login")
-    }
-
-    context.locals.user = user
-  }
-
+import type { MiddlewareHandler } from "astro"
+import { sequence } from "astro:middleware"
+import { clerkMiddleware } from "@clerk/astro/server"
+
+// Populates Astro.locals.auth() on every request. Clerk's Astro SDK
+// deliberately dropped path-based route matching here (createRouteMatcher)
+// in favor of checking auth in each protected page/route directly — see
+// src/pages/admin/*.astro and src/pages/admin/login.astro.
+const clerk = clerkMiddleware()
+
+// Never let a CDN, proxy, or the browser cache an authenticated admin
+// response — a cached 200 served to the next visitor would bypass the
+// auth check in the page entirely.
+const noStoreForAdmin: MiddlewareHandler = async (context, next) => {
   const response = await next()
-
-  // Never let a CDN, proxy, or the browser cache an authenticated admin
-  // response — a cached 200 served to the next visitor would bypass the
-  // auth check above entirely.
-  if (isAdminPage || isProtectedApi || pathname === "/admin/login") {
+  const { pathname } = context.url
+  if (pathname === "/admin" || pathname.startsWith("/admin/")) {
     response.headers.set("Cache-Control", "no-store, private")
   }
-
   return response
-})
\ No newline at end of file
+}
+
+export const onRequest = sequence(clerk, noStoreForAdmin)
diff --git a/src/pages/admin/edit/[id].astro b/src/pages/admin/edit/[id].astro
index ff0b524..1b7e26f 100644
--- a/src/pages/admin/edit/[id].astro
+++ b/src/pages/admin/edit/[id].astro
@@ -10,9 +10,8 @@ import {
 } from "../../../lib/db"
 import { slugify, estimateReadingMinutes, excerptFromHtml } from "../../../lib/utils"
 
-if (!Astro.locals.user) {
-  return Astro.redirect("/admin/login")
-}
+const { userId, redirectToSignIn } = Astro.locals.auth()
+if (!userId) return redirectToSignIn()
 
 const id = Number(Astro.params.id)
 if (!Number.isInteger(id)) {
diff --git a/src/pages/admin/index.astro b/src/pages/admin/index.astro
index 7673120..185eb9f 100644
--- a/src/pages/admin/index.astro
+++ b/src/pages/admin/index.astro
@@ -3,9 +3,8 @@ import AdminLayout from "../../layouts/AdminLayout.astro"
 import { getAllArticles, deleteArticle, getArticleById } from "../../lib/db"
 import { formatDate } from "../../lib/utils"
 
-if (!Astro.locals.user) {
-  return Astro.redirect("/admin/login")
-}
+const { userId, redirectToSignIn } = Astro.locals.auth()
+if (!userId) return redirectToSignIn()
 
 let notice = ""
 
diff --git a/src/pages/admin/login.astro b/src/pages/admin/login.astro
index e835bba..d073d47 100644
--- a/src/pages/admin/login.astro
+++ b/src/pages/admin/login.astro
@@ -1,34 +1,11 @@
 ---
 import AdminLayout from "../../layouts/AdminLayout.astro"
-import { getSessionUser, verifyCredentials, createSession } from "../../lib/auth"
-
-let error = ""
+import { SignIn } from "@clerk/astro/components"
 
 // Already signed in? Go to dashboard.
-try {
-  if (await getSessionUser(Astro.cookies)) {
-    return Astro.redirect("/admin")
-  }
-} catch (err){
-  console.error("[login] session check failed:", err)
-}
-
-if (Astro.request.method === "POST") {
-  try {
-    const form = await Astro.request.formData()
-    const email = String(form.get("email") || "").trim()
-    const password = String(form.get("password") || "")
-
-    const user = await verifyCredentials(email, password)
-    if (user) {
-      await createSession(Astro.cookies, user.id)
-      return Astro.redirect("/admin")
-    }
-    error = "Incorrect email or password. Please try again."
-  } catch (err) {
-    console.error("[login] sign-in failed:", err)
-    error = "We couldn't sign you in right now. Please try again in a moment."
-  }
+const { userId } = Astro.locals.auth()
+if (userId) {
+  return Astro.redirect("/admin")
 }
 ---
 
@@ -42,51 +19,20 @@ if (Astro.request.method === "POST") {
         </a>
       </div>
 
-      <div class="rounded-xl border border-border bg-background p-8 shadow-sm">
-        <h1 class="font-heading text-lg font-800 text-foreground">Sign in</h1>
-        <p class="mt-1 text-sm text-muted-foreground">Enter your password to manage blog posts.</p>
-
-        {error && (
-          <p class="mt-5 rounded-md border border-accent/30 bg-accent/10 px-3 py-2.5 text-sm text-accent">
-            {error}
-          </p>
-        )}
-
-        <form method="POST" class="mt-6 space-y-4">
-          <div>
-            <label for="email" class="mb-1.5 block font-heading text-xs font-600 uppercase tracking-wide text-muted-foreground">
-              Email
-            </label>
-            <input
-                type="email"
-                id="email"
-                name="email"
-                required
-                autocomplete="username"
-                class="w-full rounded-md border border-border bg-background px-3 py-2.5 text-sm text-foreground outline-none transition-colors focus:border-accent"
-            />
-          </div>
-          <div>
-            <label for="password" class="mb-1.5 block font-heading text-xs font-600 uppercase tracking-wide text-muted-foreground">
-              Password
-            </label>
-            <input
-              type="password"
-              id="password"
-              name="password"
-              required
-              autocomplete="current-password"
-              class="w-full rounded-md border border-border bg-background px-3 py-2.5 text-sm text-foreground outline-none transition-colors focus:border-accent"
-            />
-          </div>
-          <button
-            type="submit"
-            class="w-full rounded-md bg-accent px-4 py-2.5 font-heading text-sm font-700 uppercase tracking-wide text-white transition-colors hover:bg-accent-hover"
-          >
-            Sign in
-          </button>
-        </form>
-      </div>
+      <SignIn
+        forceRedirectUrl="/admin"
+        appearance={{
+          elements: {
+            rootBox: "w-full",
+            card: "rounded-xl border border-border bg-background p-8 shadow-sm w-full",
+            headerTitle: "font-heading text-lg font-800 text-foreground",
+            headerSubtitle: "text-sm text-muted-foreground",
+            formButtonPrimary:
+              "rounded-md bg-accent px-4 py-2.5 font-heading text-sm font-700 uppercase tracking-wide text-white hover:bg-accent-hover",
+            footerAction: "hidden",
+          },
+        }}
+      />
 
       <p class="mt-6 text-center text-xs text-muted-foreground">
         <a href="/" class="transition-colors hover:text-accent">← Back to website</a>
diff --git a/src/pages/admin/new.astro b/src/pages/admin/new.astro
index 24e61e3..a4e7db2 100644
--- a/src/pages/admin/new.astro
+++ b/src/pages/admin/new.astro
@@ -4,9 +4,8 @@ import EditorForm from "../../components/EditorForm.astro"
 import { createArticle, slugExists, getPublishedCategories, type ArticleInput } from "../../lib/db"
 import { slugify, estimateReadingMinutes, excerptFromHtml } from "../../lib/utils"
 
-if (!Astro.locals.user) {
-  return Astro.redirect("/admin/login")
-}
+const { userId, redirectToSignIn } = Astro.locals.auth()
+if (!userId) return redirectToSignIn()
 
 let error = ""
 const categories = await getPublishedCategories()
diff --git a/src/pages/api/logout.ts b/src/pages/api/logout.ts
deleted file mode 100644
index c86056a..0000000
--- a/src/pages/api/logout.ts
+++ /dev/null
@@ -1,7 +0,0 @@
-import type { APIRoute } from "astro"
-import { destroySession } from "../../lib/auth"
-
-export const POST: APIRoute = async ({ cookies, redirect }) => {
-  await destroySession(cookies)
-  return redirect("/admin/login")
-}
\ No newline at end of file
diff --git a/src/pages/blog/[slug].astro b/src/pages/blog/[slug].astro
index 46cd38e..0924ce1 100644
--- a/src/pages/blog/[slug].astro
+++ b/src/pages/blog/[slug].astro
@@ -13,7 +13,7 @@ if (!article || article.status !== "published") {
   return new Response(null, { status: 404, statusText: "Not Found" })
 }
 
-const related = await getRelatedArticles(article.category, article.id)
+const related = await getRelatedArticles(article.category, article.id, 5)
 const safeContent = sanitizeArticleHtml(article.content)
 
 const siteUrl = Astro.site?.toString().replace(/\/$/, "") || Astro.url.origin
@@ -77,54 +77,64 @@ const jsonLd = {
     </header>
 
     <!-- Body -->
-    <article class="mx-auto max-w-3xl px-6 py-14">
-      <p class="mb-10 text-balance text-lg font-500 leading-relaxed text-foreground">
-        {article.excerpt}
-      </p>
-      <div class="article-body" set:html={safeContent} />
+    <div class="mx-auto max-w-6xl px-6 py-14">
+      <div class="lg:grid lg:grid-cols-[minmax(0,1fr)_320px] lg:items-start lg:gap-12">
+        <article class="max-w-2xl">
+          <p class="mb-10 text-balance text-lg font-500 leading-relaxed text-foreground">
+            {article.excerpt}
+          </p>
+          <div class="article-body" set:html={safeContent} />
 
-      <!-- Share / keywords -->
-      {article.meta_keywords && (
-        <div class="mt-12 flex flex-wrap gap-2 border-t border-border pt-8">
-          {article.meta_keywords.split(",").map((k) => (
-            <span class="rounded-full bg-muted px-3 py-1 text-xs font-500 text-muted-foreground">
-              {k.trim()}
-            </span>
-          ))}
-        </div>
-      )}
-
-      <div class="mt-10">
-        <a href="/blog" class="inline-flex items-center gap-2 font-heading text-sm font-700 uppercase tracking-wide text-accent transition-colors hover:text-accent-hover">
-          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true"><path d="M19 12H5M11 6l-6 6 6 6"/></svg>
-          Back to Blog
-        </a>
-      </div>
-    </article>
+          <!-- Share / keywords -->
+          {article.meta_keywords && (
+            <div class="mt-12 flex flex-wrap gap-2 border-t border-border pt-8">
+              {article.meta_keywords.split(",").map((k) => (
+                <span class="rounded-full bg-muted px-3 py-1 text-xs font-500 text-muted-foreground">
+                  {k.trim()}
+                </span>
+              ))}
+            </div>
+          )}
 
-    <!-- Related -->
-    {related.length > 0 && (
-      <section class="border-t border-border bg-muted py-16">
-        <div class="mx-auto max-w-7xl px-6">
-          <h2 class="mb-10 text-center font-heading text-2xl font-800 text-foreground">Related Articles</h2>
-          <div class="grid gap-8 sm:grid-cols-2 lg:grid-cols-3">
-            {
-              related.map((a) => (
-                <a href={`/blog/${a.slug}`} class="group flex flex-col overflow-hidden rounded-xl border border-border bg-background shadow-sm transition-shadow hover:shadow-xl">
-                  <div class="relative aspect-[16/10] overflow-hidden">
-                    <img src={a.cover_image || "/images/hero.png"} alt={a.title} class="h-full w-full object-cover transition-transform duration-700 group-hover:scale-105" loading="lazy" />
-                  </div>
-                  <div class="flex flex-1 flex-col p-6">
-                    <p class="font-heading text-xs font-600 uppercase tracking-wide text-muted-foreground">{formatDate(a.published_at)}</p>
-                    <h3 class="mt-2 text-balance font-heading text-lg font-700 leading-snug text-foreground transition-colors group-hover:text-accent">{a.title}</h3>
-                  </div>
-                </a>
-              ))
-            }
+          <div class="mt-10">
+            <a href="/blog" class="inline-flex items-center gap-2 font-heading text-sm font-700 uppercase tracking-wide text-accent transition-colors hover:text-accent-hover">
+              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true"><path d="M19 12H5M11 6l-6 6 6 6"/></svg>
+              Back to Blog
+            </a>
           </div>
-        </div>
-      </section>
-    )}
+        </article>
+
+        <!-- Related -->
+        {related.length > 0 && (
+          <aside class="mt-14 lg:mt-0" aria-labelledby="related-articles-heading">
+            <div class="lg:sticky lg:top-8">
+              <h2 id="related-articles-heading" class="mb-5 font-heading text-xs font-700 uppercase tracking-wide text-muted-foreground">
+                Related Articles
+              </h2>
+              <div class="space-y-4">
+                {
+                  related.map((a) => (
+                    <a href={`/blog/${a.slug}`} class="group flex gap-3 rounded-xl border border-border bg-background p-3 transition-shadow hover:shadow-lg">
+                      <div class="relative h-16 w-20 flex-shrink-0 overflow-hidden rounded-lg">
+                        <img src={a.cover_image || "/images/hero.png"} alt="" class="h-full w-full object-cover transition-transform duration-700 group-hover:scale-105" loading="lazy" />
+                      </div>
+                      <div class="min-w-0">
+                        <h3 class="text-balance font-heading text-sm font-700 leading-snug text-foreground transition-colors group-hover:text-accent line-clamp-2">
+                          {a.title}
+                        </h3>
+                        <p class="mt-1 text-xs leading-relaxed text-muted-foreground line-clamp-2">
+                          {a.excerpt}
+                        </p>
+                      </div>
+                    </a>
+                  ))
+                }
+              </div>
+            </div>
+          </aside>
+        )}
+      </div>
+    </div>
   </main>
 
   <SiteFooter />
